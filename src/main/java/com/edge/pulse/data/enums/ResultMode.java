package com.edge.pulse.data.enums;

/** How a scale's result is released. IMMEDIATE = score when its items complete in any session;
 *  CONSOLIDATED = score only when the full required item set has accrued across micro-sessions. */
public enum ResultMode { IMMEDIATE, CONSOLIDATED }
