# Template only — DO NOT commit real values. Create the live secret with kubectl:
#   kubectl create secret generic pulse-secrets -n pulse \
#     --from-literal=DB_PASSWORD=... --from-literal=JWT_SECRET=... \
#     --from-literal=ENCRYPTION_KEY=<64-char-hex> --from-literal=REDIS_PASSWORD=<copied-from-xdeal>
apiVersion: v1
kind: Secret
metadata:
  name: pulse-secrets
  namespace: pulse
type: Opaque
stringData:
  DB_PASSWORD: "REPLACE_ME"
  JWT_SECRET: "REPLACE_ME_min_32_chars"
  ENCRYPTION_KEY: "REPLACE_ME_64_char_hex_32_bytes"
  REDIS_PASSWORD: "REPLACE_ME_same_as_xdeal"
