# Attention to Detail — sample package

- **Type:** COGNITIVE  ·  timed (set a time limit on import)
- **Picture-based:** yes — includes images.zip
- **Questions:** 15  ·  **Scales:** 1  ·  **Keyed:** yes (answer_key.csv)

Spot-the-difference picture items (count differences between two images). One sum scale, keyed.

**Files:** `questions.csv`, `answer_key.csv`, `scoring_sheet.csv`, `images.zip`, `sample_responses.csv`, `expected_scores.csv`

**Import:** Assessments → Import assessment → type **Cognitive ability**, attach the CSVs + images.zip, set a time limit. Step-by-step with screenshots: see [`../README.md`](../README.md).

**Derive answers:** `sample_responses.csv` holds demo candidate answers; `expected_scores.csv` the STEN scores they derive to.
